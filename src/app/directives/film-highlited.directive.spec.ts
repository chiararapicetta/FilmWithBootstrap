import { FilmHighlitedDirective } from './film-highlited.directive';

describe('FilmHighlitedDirective', () => {
  it('should create an instance', () => {
    const directive = new FilmHighlitedDirective();
    expect(directive).toBeTruthy();
  });
});
